{
  "name": "task-manager",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "docker compose up postgres -d && turbo dev",
    "build": "turbo build",
    "clean": "turbo clean",
    "test": "turbo test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "turbo": "^2.5.0",
    "typescript": "^5.8.3"
  }
}
